import 'package:flutter/material.dart';

class SWColors {
  // static bool isMan = false;
  static const themeColor = Color(0xFFFeFeFe);
  static const themeText = Color(0xff0F0F0F);
  static const themeSecond = Color(0xff2E3030);
  static const shadowColor = Color(0xfff1f1f1);
  static const greyColor = Color(0xFF999999);
  static const gregBg = Color(0xFFD3D3D3);
  static const goldColor = Color(0xFFF0C18B);
  static const goldRed = Color(0xffd04b40);
  static const green = Color(0xff3BA538);

  static const modelGrayColor = Color(0xFF1A1A1A);
  static const color80000000 = Color(0x80000000);

  // 自己的主题
  // 白色
  static const colorWhite = Color(0xFFFFFFFF);

  // 黑色 #262334
  static const colorBlack = Color(0xFF262334);

  // 按钮黑 #524f5f
  static const colorBtnBlack = Color(0xFF524f5f);

  // formItem 容器框
  static const colorFormBlack = Color(0xff514f5d);

  // 黑色字体
  static const textColorBlack = Color(0xff28233D);
}
